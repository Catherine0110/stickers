mixin svg(className,name,width, height)
    - svgWidth = width || '100%'
    - svgHeight = height || '100%'
    svg(width=`${svgWidth}` height=`${svgHeight}` class="icon "+className)
        use(xlink:href="assets/images/sprite.svg#" + name)